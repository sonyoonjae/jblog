<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cate">
	
	<!-- 카테고리저장 -->
	<insert id="insert" parameterType="com.javaex.vo.CateVo">
		<selectKey keyProperty="cateNo" resultType="int" order="BEFORE">
			select SEQ_CATEGORY_CATENO.nextval from dual
		</selectKey>
		<![CDATA[
			insert into category
			values(#{cateNo}, 
			       #{userNo},
			       #{cateName}, 
			       #{description},
			       sysdate
			)
		]]>
	</insert>
	
	<!-- 특정카테고리 정보를 가져온다 -->
	<select id="selectCate" parameterType="int" resultType="com.javaex.vo.CateVo">
		<![CDATA[
			select cateNo, userNo, cateName, description, regDate
			from category
			where cateNo =#{cateNo}
			
		]]>
	</select>
	
	<!-- 회원번호로 가테고리리스트 가져오기 (포스트 갯수도 같이 가져옴)-->
	<select id="selectList" parameterType="int" resultType="com.javaex.vo.CateVo">
		<![CDATA[
			select c.cateNo, c.userNo, c.cateName, c.description, c.regDate, p.cnt
			from category c, 
     			 (select cateNo, count(*) cnt
      			  from post
      			  group by cateNo) p
			where userNo = #{userNo}
		    and c.cateNo = p.cateNo(+)
			order by cateNo desc
			
			
		]]>
	</select>
	
	
	<!-- 카테고리삭제 -->
	<delete id="deleteCate" parameterType="int" >
		<![CDATA[
			delete from category
			where cateNo =#{cateNo}
		]]>
	</delete>
	
</mapper>